% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Showing_charts.R
\name{confronto_lag}
\alias{confronto_lag}
\title{Plot Lag-Response Comparison Curves Across Species Groups for a Given SPEI Value}
\usage{
confronto_lag(lista_pred, valore_spei, nomi_gruppi, lag_n = 24, font = "bold")
}
\arguments{
\item{lista_pred}{A \code{list} of \code{crosspred} objects generated by \code{dlnm::crosspred()}
or \code{get_dlnm_pred()}, each corresponding to a specific species group or model.}

\item{valore_spei}{Numeric vector or single numeric value specifying the SPEI target value(s) to slice
from the prediction matrix.}

\item{nomi_gruppi}{Character vector of group names corresponding to each prediction object in \code{lista_pred}.}

\item{lag_n}{Integer. Total number of monthly lags included in the prediction matrix. Default is \code{24}.}

\item{etichette_mesi}{Character vector of month labels used to annotate the reversed lag x-axis
(must contain at least \code{lag_n} labels).}
}
\value{
A \code{ggplot} object displaying lag-response curves (fitted values and confidence ribbons)
for each group, centered around a reference baseline (y = 1).
}
\description{
Extracts lag-response predictions from a list of fitted DLNM predictions (\code{crosspred} objects)
at a specified climate anomaly threshold (\code{valore_spei}). Binds prediction curves and
confidence intervals across groups, reverses the x-axis to represent chronological lag steps
using custom month labels, and generates a comparative \code{ggplot2} visualization.
}
\examples{
\dontrun{
library(dplyr)
library(ggplot2)
library(dlnm)

# Mock prediction objects with required matRR matrices
mock_pred <- list(
  predvar = seq(-2, 2, length.out = 10),
  matRRfit = matrix(runif(240, 0.8, 1.2), nrow = 10, ncol = 24),
  matRRlow = matrix(runif(240, 0.6, 0.9), nrow = 10, ncol = 24),
  matRRhigh = matrix(runif(240, 1.1, 1.4), nrow = 10, ncol = 24)
)

month_labels <- paste0("M", 1:24)

p <- confronto_lag(
  lista_pred = list(mock_pred),
  valore_spei = 0,
  nomi_gruppi = "Fagus",
  etichette_mesi = month_labels,
  lag_n = 24
)

print(p)
}

}
