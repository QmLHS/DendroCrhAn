% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Showing_charts.R
\name{grafico_unito}
\alias{grafico_unito}
\title{Plot Side-by-Side DLNM Contour Surfaces Across Multiple Models}
\usage{
grafico_unito(..., nomi, titolo, font = "bold", n_righe = 1)
}
\arguments{
\item{...}{Multiple \code{crosspred} objects passed as separate arguments.}

\item{nomi}{Character vector of labels for each model corresponding to the order of objects passed in \code{...}.
If \code{NULL}, names are extracted automatically from input object expressions.}

\item{titolo}{Character string specifying the main title of the generated plot.}

\item{mesi}{Character vector of labels used to annotate the discrete monthly lag steps on the y-axis.}
}
\value{
A \code{ggplot} object containing faceted 2D filled contour plots of exposure-lag response surfaces
with a shared horizontal color scale at the bottom.
}
\description{
Combines multiple DLNM prediction objects (\code{crosspred}) into a unified, faceted 2D contour plot.
Visualizes the joint exposure-lag-response surfaces (e.g., SPEI vs. monthly lags) side-by-side using
a symmetrical, zero-centered color palette across all specified model comparisons.
}
\examples{
\dontrun{
library(ggplot2)
library(dlnm)

# Mock cross-basis prediction objects
mock_pred1 <- list(
  predvar = seq(-2, 2, length.out = 15),
  matfit = matrix(rnorm(15 * 12, mean = 0, sd = 0.5), nrow = 15, ncol = 12)
)
mock_pred2 <- list(
  predvar = seq(-2, 2, length.out = 15),
  matfit = matrix(rnorm(15 * 12, mean = 0.2, sd = 0.4), nrow = 15, ncol = 12)
)

month_names <- paste0("M", 1:12)

p <- grafico_unito(
  mock_pred1, mock_pred2,
  nomi = c("Site A", "Site B"),
  mesi = month_names,
  titolo = "SPEI Lag-Response Surfaces"
)

print(p)
}

}
